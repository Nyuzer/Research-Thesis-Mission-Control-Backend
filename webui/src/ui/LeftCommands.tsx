// LeftCommands is no longer used — Stop/Abort has been moved to TopBanner.
// This file is kept as an empty export to avoid breaking any remaining imports
// during the transition. It can be safely deleted.
export default function LeftCommands() {
  return null;
}
